流程控制语句
顺序
条件分支
    if...elif...else
循环》减少重复
while。。。else
break 跳出循环 所在循环结束
for i in range(...)
pass 》{}
continue 中断当前循环，直接开始下次循环
数据类型
数字
字符
布尔
运算符
    算术运算符+-*/%  // **
    关系运算符 == > >= < <= !=
    位运算符 & | ^ ~ >>   <<
    赋值运算符 +=
    身份运算符 is
    成员运算符 in
    三目运算符
列表
    [],list([])
    len() 长度
    索引
    切片》列表
    增删改查
    增加：append insert
    删除：remove pop pop(index) del
    改：索引，index()
    查:遍历
元祖
字典
集合